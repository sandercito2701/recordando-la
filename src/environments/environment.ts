// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.



/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCREjRz2R5Z7-cDGkVX04B0yCisWzlP4fU",
    authDomain: "recordando-la-f9e84.firebaseapp.com",
    databaseURL: "https://recordando-la-f9e84.firebaseio.com",
    projectId: "recordando-la-f9e84",
    storageBucket: "recordando-la-f9e84.appspot.com",
    messagingSenderId: "1003993050318"
  }
};